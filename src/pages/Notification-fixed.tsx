// This file exists only as a compatibility layer
// We're redirecting all traffic to the App Router version
// through middleware and client-side navigation
export default function NotificationPage() {
  return null;
}